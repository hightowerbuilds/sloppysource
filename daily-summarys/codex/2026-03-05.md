# Codex Daily Summary - 2026-03-05

## Objectives
- Answer how to install additional Codex skills.

## Work Log
- Used the `skill-installer` skill workflow because the request is about adding more skills.
- Reviewed `SKILL.md` at `/Users/lukehightower/.codex/skills/.system/skill-installer/SKILL.md`.
- Verified today summary file was missing and created this file per repo instructions.
- Attempted to list curated skills using:
  - `python3 /Users/lukehightower/.codex/skills/.system/skill-installer/scripts/list-skills.py`
- Script failed in this environment due TLS certificate verification error.
- Switched to GitHub API fallback (`curl`) to retrieve available curated skills and continue answering.
- Retrieved curated skills from:
  - `https://api.github.com/repos/openai/skills/contents/skills/.curated?ref=main`
- Confirmed currently installed local skills:
  - `playwright`
  - `sentry`
- User requested adding `gh-cli` skill from `https://skills.sh/github/awesome-copilot/gh-cli` without changing app code.
- Ran Bun installer:
  - `bunx skills add https://github.com/github/awesome-copilot --skill gh-cli --agent codex --yes`
- Installer created:
  - `.agents/skills/gh-cli/SKILL.md`
  - `skills-lock.json`
- Mirrored installed skill into repo `skills/` folder:
  - `skills/gh-cli/SKILL.md`

## Additional Work (Project Flow Hardening + GitHub Activity View)
- Hardened shared GitHub runtime in `supabase/functions/_shared/projectRuntime.ts`:
  - Added GitHub API request wrapper with retry/backoff for `429`/`5xx`.
  - Added structured GitHub response metadata capture (request id + rate-limit headers).
  - Added `GitHubApiError` and `mapGitHubErrorToHttp` for consistent status/message mapping.
  - Exposed `githubGetJson` helper for typed GitHub read endpoints.
- Updated `project-create` edge function:
  - Added idempotency reuse path for active `sessionKey` + user.
  - Added richer audit logs across success/reused/error phases.
  - Added classified GitHub error responses for create failures.
- Added new edge function: `supabase/functions/project-github-activity/index.ts`
  - Authenticated user-owned project lookup.
  - On-demand GitHub fetch for PRs, workflow runs/checks, and recent commits.
  - Returns normalized activity payload plus local `project_audit_logs` timeline.
  - Writes success/error audit entries for activity pulls.
- Added migration: `supabase/migrations/20260305_project_session_key_idempotency.sql`
  - Unique active session key index per user for dedupe safety.
- Added frontend activity experience:
  - New route/page: `src/pages/ProjectActivityPage.tsx` (+ css).
  - New models in `src/lib/projectModels.ts` for activity payload.
  - New API call in `src/lib/projectsDb.ts`: `getProjectGitHubActivity`.
  - New query key in `src/lib/queryKeys.ts`.
  - Added Activity link from each project card in `src/pages/ProjectPage.tsx`.
  - Added router entry in `src/router.tsx` for `/project/$projectId/activity`.
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Remote Migration + Function Deploy)
- Ran remote migration deployment:
  - `supabase db push --include-all`
  - Applied migration: `20260305_project_session_key_idempotency.sql`
- Deployed updated edge functions with `--no-verify-jwt`:
  - `project-create`
  - `project-export-zip`
  - `project-end-session`
  - `project-cleanup`
  - `project-github-activity` (new)
- Verified remote function status with:
  - `supabase functions list --project-ref gbnizxzurmbzeelacztr`
  - Confirmed all project lifecycle functions are `ACTIVE`.

## Additional Work (Live Smoke Test)
- Ran end-to-end live smoke test against deployed endpoints using a temporary auth user:
  1. Create auth user (admin API) -> `200`
  2. Password login -> `200`
  3. `project-create` -> `200`
  4. `project-github-activity` -> `200`
  5. `project-end-session` -> `200`
  6. Project row verification via REST -> `200`
  7. Delete temp auth user -> `200`
- Smoke-test project:
  - `projectId`: `daee1ed8-4817-423a-8103-fae4af71d3f1`
  - `repoFullName`: `hightowerbuilds/sloppy-project-codex-smoke-activity-100501-a081e3d6`
- Activity payload counts:
  - PRs: `0`
  - Checks: `0`
  - Commits: `1`
  - Audit events: `0`
- Final project lifecycle state after end-session:
  - `status=deleted`
  - `last_error` empty

## Additional Work (Navbar Label + Order Update)
- Updated `src/components/Navbar.tsx` navbar link ordering/labels per request:
  - Moved project link to sit directly after Home.
  - Renamed `Project` to `Projects`.
  - Renamed `Upload` to `MDs`.
- Kept existing routes unchanged (`/project`, `/upload`).
- Validation:
  - `bun run lint` passed.

## Additional Work (MDs Dropdown Navigation)
- Updated navbar to make `MDs` a click dropdown in `src/components/Navbar.tsx`.
- Added dropdown menu options with exact visible labels and routes:
  - `/upload` -> `/upload`
  - `/display` -> `/display`
  - `/search` -> `/search`
- Removed standalone top-level `Display` and `Search` buttons from navbar and grouped them under `MDs`.
- Added dropdown menu styling in `src/components/Navbar.css`.
- Validation:
  - `bun run lint` passed.

## Additional Work (Project List State Management Fix)
- Investigated why ended projects with `status=deleted` remained visible on `/project`.
- Root cause: `listProjects()` fetched all statuses, including `deleted`.
- Updated `src/lib/projectsDb.ts` to exclude deleted rows:
  - Added `.neq("status", "deleted")` to the `projects` select query.
- Added UI-side guard in `src/pages/ProjectPage.tsx`:
  - Filters out `status === "deleted"` before rendering project cards, preventing stale-cache display.
- Result: once a project reaches `deleted`, it no longer appears in the Projects page list.
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Navbar Data-Bar Hover Color)
- Updated `src/components/Navbar.css` to stop text color changes on storage/data bar hover.
- Removed hover color overrides from:
  - `.navbar-storage:hover`
  - `.navbar-storage:hover .navbar-storage-label`
- Kept hover background highlight behavior unchanged.
- Validation:
  - `bun run lint` passed.

## Additional Work (Navbar Hover/Active Color Match)
- Adjusted `.navbar-storage:hover` background in `src/components/Navbar.css` to exactly match the active nav highlight color.
- Changed value from `rgba(144, 238, 144, 0.5)` to `rgba(144, 238, 144, 0.25)`.
- Validation:
  - `bun run lint` passed.

## Additional Work (Navbar Data Bar Routing)
- Updated `src/components/Navbar.tsx` to make the storage/data bar a route link:
  - Changed container from `<div className="navbar-storage">` to `<Link to="/upload" className="navbar-storage">`.
- Updated `src/components/Navbar.css` to preserve anchor styling consistency:
  - Added `color: inherit;`
  - Added `text-decoration: none;`
- Result: clicking the data bar now navigates to `/upload`.
- Validation:
  - `bun run lint` passed.

## Additional Work (Data Bar Relocation to Upload Page)
- Reverted prior navbar data-bar link behavior by removing storage bar rendering from `src/components/Navbar.tsx`.
- Removed now-unused `navbar-storage*` styles from `src/components/Navbar.css`.
- Moved storage usage bar into `/upload` page header (`src/pages/UploadPage.tsx`), positioned in the upper-right section next to the `Upload .md` button.
- Added storage query to Upload page using `getUserStorageUsage` and `storageUsageQueryKey`.
- Added new Upload page styles in `src/pages/UploadPage.css` for:
  - header left/right layout
  - upload storage bar label + progress track/fill
  - responsive wrapping behavior on smaller widths
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Upload Header Layout Order)
- Updated `/upload` header layout to match requested order:
  1. Upload button on the left
  2. `MD Files: <count>` directly next to it
  3. Storage/data bar spanning the remaining width
- Implemented in:
  - `src/pages/UploadPage.tsx` (header structure)
  - `src/pages/UploadPage.css` (flex behavior + typography)
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Upload Button Radius)
- Updated shared `.upload-button` style in `src/pages/UploadPage.css`:
  - Changed `border-radius` from `8px` to `0`.
- Validation:
  - `bun run lint` passed.

## Additional Work (Upload Button Hover Glow)
- Added hover glow animation for `.upload-button` in `src/pages/UploadPage.css`.
- Added:
  - hover animation trigger (`upload-button-glow`)
  - animated box-shadow keyframes
  - disabled-state guard to prevent glow while disabled
- Validation:
  - `bun run lint` passed.

## Additional Work (Revert Upload Button Glow)
- Reverted the upload-button hover glow animation in `src/pages/UploadPage.css`.
- Restored simple hover behavior (`opacity` only), removing glow keyframes and animation rules.
- Validation:
  - `bun run lint` passed.

## Additional Work (Reusable TV Static Effect + Upload Hover)
- Added reusable fine-grain static effect utilities in `src/index.css`:
  - `.fx-tv-static`
  - `.fx-tv-static-hover`
  - `@keyframes tv-static-shift`
- Applied effect to Upload button hover by updating button classes in `src/pages/UploadPage.tsx`:
  - `fx-tv-static fx-tv-static-hover`
- Tuned upload-button hover opacity in `src/pages/UploadPage.css` from `0.74` to `0.9` so the background static texture remains visible.
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Home Route Thumbnail Static Hover)
- Applied reusable static hover effect to Home route thumbnail cards in `src/pages/HomePage.tsx` by adding:
  - `fx-tv-static fx-tv-static-hover`
- Updated `src/pages/HomePage.css` hover style to use `background-color` (instead of `background` shorthand) so static texture layers remain visible.
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Static Effect Color Adjustment)
- Updated reusable static utility in `src/index.css` to pure black-and-white noise (removed blue tint layer).
- Rebalanced white/black speckle contrast and kept fine-grain animation behavior.
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Replicate hightowerbuilds.com Blog Static Technique)
- Inspected deployed `https://www.hightowerbuilds.com/blog` assets and confirmed the static effect approach:
  - SVG `feTurbulence` texture encoded as `data:image/svg+xml`
  - Applied via `backgroundImage` on `.tv-static-inner`
  - Fast jitter animation for movement (`static-noise` keyframes)
- Re-implemented this approach in our reusable utility at `src/index.css`:
  - Added `--fx-tv-static-image` CSS variable containing encoded SVG turbulence noise
  - Updated `.fx-tv-static*` classes to use that texture on hover/active states
  - Updated `tv-static-shift` keyframes to the same style of jitter movement
- Existing usage points (Upload button + Home route cards) now use this upgraded implementation through the same utility classes.
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Homepage Route Thumbnail Corners)
- Updated homepage route thumbnail/button styling in `src/pages/HomePage.css`:
  - Changed `.home-landing-card` `border-radius` from `10px` to `0`.
- Scope limited to homepage only.
- Validation:
  - `bun run lint` passed.

## Additional Work (Homepage Thumbnail Label)
- Updated homepage route thumbnail title in `src/pages/HomePage.tsx`:
  - Changed `Project` to `Projects`.
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Page Intro Copy)
- Updated Project page intro text in `src/pages/ProjectPage.tsx` under the page title.
- Replaced prior subtitle with requested SloppySource baton-passing guidance:
  - 24-hour member window
  - unlimited commit/push during that window
  - allowed to wait until last moment to push
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Intro Container Width)
- Wrapped Project intro copy in dedicated container in `src/pages/ProjectPage.tsx`:
  - Added `.project-page-intro` around subtitle text.
- Styled intro container width in `src/pages/ProjectPage.css`:
  - Desktop: `66.666%` of outer container width
  - Mobile (`max-width: 760px`): `100%`
- Centered the intro container horizontally with `margin: 0 auto`.
- Validation:
  - `bun run lint` passed.
  - `bun run build` passed.

## Additional Work (Project Intro Guidelines Copy)
- Expanded Project intro copy in `src/pages/ProjectPage.tsx` with requested rules.
- Added two line breaks after the initial baton-passing paragraph.
- Added guideline block text:
  - shared DB recommendation (Supabase)
  - no repo assets, cloud storage policy
  - KISSgf method statement
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Intro Anticipation Paragraph)
- Added a new follow-up paragraph to Project intro text in `src/pages/ProjectPage.tsx`:
  - `Here's what to anticipate:`
  - `- commit messages will display in real-time`
- Added second anticipation bullet:
  - `- the timeclock will countdown and notify all members the baton will pass soon`
- Added third anticipation bullet:
  - `- users will be able to see the site when the builder pushes`
- Updated wording in third bullet:
  - changed `users` to `the group`
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Intro Font Size)
- Updated `src/pages/ProjectPage.css`:
  - Set `.project-page-subtitle` font size to `18px`.
- Scope applies to the Project intro paragraph block (including added guideline/anticipation lines).
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Intro Guideline #4)
- Updated Project intro guideline list in `src/pages/ProjectPage.tsx`:
  - Added `4. the builder should begin on builder branch (dev).`
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Guideline #4 Code Block)
- Reworked Project intro content structure in `src/pages/ProjectPage.tsx` to support a real code block under guideline `4`.
- Added code block with requested commands:
  - `git checkout main`
  - `git merge builder`
  - `git push origin main`
- Added intro code-block styles in `src/pages/ProjectPage.css`:
  - `.project-page-intro-code`
  - `.project-page-intro-code code`
- Also set `.project-page-intro` to a grid layout for clean vertical spacing between paragraphs and code block.
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Guideline #4 Wording Update)
- Updated guideline `4.` text in `src/pages/ProjectPage.tsx` to:
  - `the builder should begin on the builder branch and end on the main branch which is hosted.`
- Validation:
  - `bun run lint` passed.

## Additional Work (Project Guideline #4 Phrase Removal)
- Updated guideline `4.` in `src/pages/ProjectPage.tsx` by removing leading phrase:
  - removed `the builder should`
  - current line: `4. begin on the builder branch and end on the main branch which is hosted.`
- Validation:
  - `bun run lint` passed.

## Additional Work (Bun Linting Process Review)
- Reviewed lint pipeline wiring:
  - `package.json` defines `lint` as `eslint .`
  - Bun is acting as script runner (Bun v`1.3.10`), not as a separate built-in lint engine for this project.
- Ran validation commands:
  - `bun run lint` (passes)
  - `bun run lint -- --debug` (captured to `/tmp/sloppysource-eslint-debug.log`)
- Findings from debug trace:
  - ESLint flat config loaded from `eslint.config.js`
  - `globalIgnores(['dist'])` excludes `dist`
  - Rule block applies to `**/*.{ts,tsx}` only
  - 55 files were actually linted (`32 .ts`, `22 .tsx`, `1 .js`)
  - ESLint still traverses many non-TS files/directories when starting from `eslint .`, though they do not get linted by TS/TSX rules

## Additional Work (Lint Detection Verification)
- Verified ESLint is actively detecting errors in this repo:
  - Created temporary probe file `src/__lint_probe__.ts` with known violations and ran local ESLint binary.
  - ESLint reported expected errors:
    - `@typescript-eslint/no-unused-vars`
    - `prefer-const`
    - `@typescript-eslint/no-explicit-any`
  - Probe file removed after check.
- Verified no-file probe workflow via stdin:
  - `printf "...code..." | ./node_modules/.bin/eslint --stdin --stdin-filename src/__lint_probe__.ts`
  - Exit code was `1` with the same expected rule violations.
- Checked suppression sources:
  - No `.eslintignore` file present.
  - No `eslint-disable` or `@ts-ignore` suppressions found under `src/` or `supabase/`.

## Additional Work (Codebase Walkthrough)
- Performed a full repository reconnaissance for architecture and runtime flow.
- Mapped frontend stack: Vite + React 19 + TanStack Router + TanStack Query + Supabase auth/data.
- Reviewed key pages and features:
  - `/upload` document ingestion + storage quota meter.
  - `/display` viewer modes (rendered, traverse, pose, slides, token view).
  - `/search` web-worker driven markdown search with tag filters.
  - `/project` project lifecycle UI + GitHub workflow guidance.
  - `/project/$projectId/activity` GitHub PR/check/commit + audit timeline view.
- Reviewed data and integration layers:
  - `src/lib/supabaseDb.ts` (documents CRUD + quota checks).
  - `src/lib/projectsDb.ts` (project lifecycle edge function invocations).
  - `supabase/functions/*` (create/export/end-session/activity/cleanup).
  - `supabase/migrations/20260304_project_repos.sql` + `20260305_project_session_key_idempotency.sql`.
- Ran project health checks:
  - `bun run build` failed due TypeScript error in `src/__lint_probe__.ts` (`unused` variable).
  - `bun run lint` failed with 3 expected lint violations in `src/__lint_probe__.ts`.
- No code edits were made outside of this daily summary update.

## Additional Work (Project Thumbnails + Activity Control Move)
- Refactored `/project` page (`src/pages/ProjectPage.tsx`, `src/pages/ProjectPage.css`) to show project thumbnails/cards instead of action-heavy rows.
- Each project card now links directly to `/project/$projectId/activity`.
- Thumbnail/card content now includes requested items:
  - project status (e.g. `active`)
  - 24-hour clock countdown/expiry label
  - project title
  - repo URL text
- Moved operational controls from `/project` to activity page:
  - `Export ZIP`
  - `End Session`
  - full `Next Steps` workflow block (clone, branch, PR actions + copy buttons)
- Implemented move in `src/pages/ProjectActivityPage.tsx` and `src/pages/ProjectActivityPage.css`.
- Added project detail query on activity page via `listProjects()` to render current project status/title/clock/repo while keeping existing GitHub activity panels.
- Validation:
  - Targeted lint passed: `./node_modules/.bin/eslint src/pages/ProjectPage.tsx src/pages/ProjectActivityPage.tsx`
  - Full build still fails due pre-existing probe file: `src/__lint_probe__.ts`.

## Additional Work (Activity Sidebar Workspace)
- Updated `/project/$projectId/activity` to use a single container with:
  - left sidebar listing windows
  - right main area showing selected window content
- Implemented sidebar windows for requested sections:
  - `Next Steps`
  - `Pull Requests`
  - `Checks`
  - `Lifecycle Events`
- Added window state management in `src/pages/ProjectActivityPage.tsx` (`ActivityWindow`, `activeWindow`, `selectedWindow`).
- Moved rendering of those four sections from stacked panels into conditional main-content windows.
- Added new layout and interaction styles in `src/pages/ProjectActivityPage.css`:
  - `.project-activity-workspace`
  - `.project-activity-sidebar`
  - `.project-activity-window-button` (+ active/disabled states)
  - `.project-activity-main`
- Preserved existing project header card and controls (`Export ZIP`, `End Session`) and left `Recent Commits` visible below the new workspace for now.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Activity Layout Ordering)
- Updated `src/pages/ProjectActivityPage.tsx` to move `Recent Commits` above the sidebar workspace container.
- New order on activity page:
  1. Summary
  2. Recent Commits
  3. Sidebar workspace (`Next Steps`, `Pull Requests`, `Checks`, `Lifecycle Events`)
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Commit Thumbnail Formatting)
- Updated `Recent Commits` item layout in `src/pages/ProjectActivityPage.tsx`:
  - moved commit key (`shortSha`) inline with commit time on one metadata line.
  - removed author display text from commit cards (removes `hightowerbuilds` label in UI).
- Updated `src/pages/ProjectActivityPage.css`:
  - added `.project-activity-commit-message` with `font-size: 18px`.
  - added `.project-activity-commit-line` for inline metadata row styling.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Repo Link Visual Style)
- Updated `src/pages/ProjectActivityPage.css` so the repo URL in the activity summary remains the same text color as surrounding content.
- Added `.project-activity-summary-line a { color: inherit; text-decoration: none; }` to remove default hyperlink styling while keeping the URL clickable.

## Additional Work (Sidebar Label + Border Cleanup)
- Updated `src/pages/ProjectActivityPage.tsx` to remove the `Windows` sidebar title text.
- Updated `src/pages/ProjectActivityPage.css` to remove the sidebar container border for that window-list container.
- Also removed unused `.project-activity-sidebar-title` styles.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx src/pages/ProjectActivityPage.css` (TSX clean; CSS reported as ignored by eslint config).

## Additional Work (Sidebar Radius Removal)
- Updated `src/pages/ProjectActivityPage.css` to remove `border-radius` from `.project-activity-sidebar`.

## Additional Work (Window Button Radius Removal)
- Updated `src/pages/ProjectActivityPage.css` to remove `border-radius` from `.project-activity-window-button`.

## Additional Work (Content Window Radius Removal)
- Updated `src/pages/ProjectActivityPage.css` to remove `border-radius` from `.project-activity-panel` (main content window).

## Additional Work (Projects Page Heading Update)
- Updated `src/pages/ProjectPage.tsx` header text:
  - removed `Project`
  - replaced `Project Repos` with `Sloppy Projects`
- Updated `src/pages/ProjectPage.css` to center `.project-page-title`.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Projects Heading Left Alignment)
- Updated `src/pages/ProjectPage.css` so `.project-page-title` is left-aligned and matches the intro text block width/position (`width: 66.666%`, `margin: 0 auto`).

## Additional Work (Projects Outer Border Removal)
- Updated `src/pages/ProjectPage.css` to remove the outer page container border from `.project-page`.

## Additional Work (Projects Page Left Column Layout)
- Refactored `src/pages/ProjectPage.tsx` so the top area is a dedicated left column containing:
  - heading and intro text
  - `Project Name` input
  - `Session Key` input
  - `Start Project` button
- Added new layout wrappers/classes in `src/pages/ProjectPage.css`:
  - `.project-page-top`
  - `.project-page-left-column`
- Updated title/intro/form styles to align left in that column and removed prior centered-width constraints.
- Kept the projects thumbnail panel as a separate section below so it still spans full page width.
- Added responsive fallback so top layout collapses to one column under `760px`.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Projects Intro Copy Update)
- Replaced the first intro paragraph in `src/pages/ProjectPage.tsx` with updated SloppySource turn/24hr/pull-request/end-early messaging.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Projects Intro Sentence Tweak)
- Updated intro copy in `src/pages/ProjectPage.tsx` to: `You may push a lot or just at the end.`
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Collapsible Recent Commits)
- Updated `src/pages/ProjectActivityPage.tsx` to make `Recent Commits` collapsible.
- Added `Collapse` / `Expand` toggle in the commits panel header and wired it with state (`isCommitsCollapsed`).
- Added accessible attributes on toggle (`aria-expanded`, `aria-controls`) and content container (`id="recent-commits-content"`, `hidden`).
- Added `.project-activity-collapse-button` styles in `src/pages/ProjectActivityPage.css`.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Left-Aligned Title Toggles)
- Updated `src/pages/ProjectActivityPage.tsx` so collapsible panel titles are the toggle buttons themselves (left-aligned), instead of separate right-side `Collapse/Expand` buttons.
- Applied this pattern to:
  - `Recent Commits`
  - `More GitHub`
- Updated `src/pages/ProjectActivityPage.css` with `.project-activity-panel-title-toggle` styles and removed prior right-side collapse-button styling.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Recent Commits Auto Height)
- Updated `src/pages/ProjectActivityPage.tsx` to add a dedicated class on the Recent Commits panel: `.project-activity-panel-fit`.
- Updated `src/pages/ProjectActivityPage.css` to remove fixed minimum height for that panel (`min-height: 0`) so it collapses to content/text size.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Next Steps Label Restore)
- Updated `src/pages/ProjectActivityPage.tsx` to rename `More GitHub` back to `Next Steps` in:
  - sidebar window button label
  - collapsible panel title-toggle button
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Recent Commits +/- Toggle)
- Updated `src/pages/ProjectActivityPage.tsx` so `Recent Commits` title toggle now shows a left symbol:
  - `-` when expanded
  - `+` when collapsed
- Added toggle-symbol styling in `src/pages/ProjectActivityPage.css` (`.project-activity-panel-toggle-symbol`) and inline-flex alignment on title toggle.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Activity Workspace Open/Close)
- Updated `src/pages/ProjectActivityPage.tsx` to add a top-level toggle for the full activity workspace container (sidebar + main window content for `Next Steps`, `Pull Requests`, `Checks`, `Lifecycle`).
- Added state `isWorkspaceCollapsed` and a left-side `+ / -` symbol toggle button labeled `Activity Windows`.
- Wrapped workspace content in a collapsible container (`id="activity-workspace-content"`, `hidden` when collapsed).
- Added `.project-activity-workspace-wrap` spacing styles in `src/pages/ProjectActivityPage.css`.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Activity Windows Container Styling)
- Updated `src/pages/ProjectActivityPage.tsx` so the `+ / - Activity Windows` toggle and its content are wrapped in a `project-activity-panel` container, matching the `Recent Commits` container pattern.
- Kept the workspace itself collapsible via the existing toggle state and controls.
- Updated `src/pages/ProjectActivityPage.css` to remove the old inner workspace border/background/padding so the new outer panel container is the primary visual container.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Root-Level Markdown Display on Activity Page)
- Implemented root-level markdown discovery and display for the selected remote GitHub repo.
- Backend updates:
  - Extended `supabase/functions/project-github-activity/index.ts` to include `markdownFiles` from repo root (`/contents`) filtered to root-level `.md` files.
  - Added `supabase/functions/project-github-markdown-file/index.ts` for on-demand markdown file fetch by `projectId + path` with root-level `.md` validation and base64 decode.
  - Updated `supabase/functions/README.md` to include deployment entry for `project-github-markdown-file`.
- Shared/client model updates:
  - Added `ProjectActivityMarkdownFile` and `ProjectMarkdownFileContentResponse` in `src/lib/projectModels.ts`.
  - Extended `ProjectGitHubActivityResponse` with `markdownFiles` and `githubMeta.markdownFiles`.
  - Added `projectMarkdownFileQueryKey(...)` in `src/lib/queryKeys.ts`.
  - Added `getProjectMarkdownFile(projectId, path)` in `src/lib/projectsDb.ts`.
- Activity page UI updates (`src/pages/ProjectActivityPage.tsx`, `src/pages/ProjectActivityPage.css`):
  - Added a new collapsible `Markdown Display` panel under the Activity Windows container.
  - Added browser-style tabs for root markdown files.
  - Added on-demand markdown content loading via React Query when tabs are selected.
  - Added in-panel markdown rendering using `react-markdown` + `remark-gfm`.
  - Added loading, empty, and error states for markdown list/content.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx src/lib/projectsDb.ts src/lib/projectModels.ts src/lib/queryKeys.ts` passed.
  - `bun run build` still fails only due pre-existing `src/__lint_probe__.ts` (`unused` variable), unrelated to this feature work.

## Additional Work (Supabase Function Deploys)
- Deployed updated function: `project-github-activity`.
- Deployed new function: `project-github-markdown-file`.
- Target project: `gbnizxzurmbzeelacztr`.

## Additional Work (401 Auth Header Hardening)
- Diagnosed edge 401 (`Missing authorization header`) as gateway-level missing bearer token on function requests.
- Updated `src/lib/projectsDb.ts` to explicitly attach `Authorization: Bearer <access_token>` for project function invocations:
  - `project-create`
  - `project-export-zip`
  - `project-end-session`
  - `project-github-activity`
  - `project-github-markdown-file`
- Added session access-token helper with clearer user-facing errors when session is missing/expired.
- Validation:
  - `./node_modules/.bin/eslint src/lib/projectsDb.ts src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (401 Follow-up: Session Refresh)
- Identified likely cause of repeated `401` on `project-github-activity`: stale/expired access token from `supabase.auth.getSession()`.
- Updated `requireAuthenticatedAccessToken` in `src/lib/projectsDb.ts` to:
  - confirm authenticated user via `supabase.auth.getUser()`
  - read current session
  - proactively refresh session when token is missing or near expiry (`<= 30s`)
  - use refreshed access token for all project function invocations
- Validation:
  - `./node_modules/.bin/eslint src/lib/projectsDb.ts src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Activity Page Default-Collapsed Panels)
- Updated `src/pages/ProjectActivityPage.tsx` so these sections load collapsed by default when entering the activity page:
  - `Recent Commits`
  - `Activity Windows`
- Change made by setting initial React state:
  - `isCommitsCollapsed` -> `true`
  - `isWorkspaceCollapsed` -> `true`
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.
  - `bun run lint` still fails due pre-existing `src/__lint_probe__.ts`, unrelated to this change.

## Additional Work (Markdown Display Default-Collapsed)
- Updated `src/pages/ProjectActivityPage.tsx` so `Markdown Display` also loads collapsed on entry.
- Change made by setting initial React state:
  - `isMarkdownCollapsed` -> `true`
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Activity Header Button Borders)
- Updated `src/pages/ProjectActivityPage.css` to remove borders from the activity header actions only:
  - `Back to Projects`
  - `Refresh Activity`
- Implemented as a scoped override under `.project-activity-header-actions` so other activity page buttons retain their existing borders.

## Additional Work (Markdown Titles Visible While Collapsed)
- Updated `src/pages/ProjectActivityPage.tsx` so when `Markdown Display` is collapsed, the panel header still shows the available markdown filenames.
- Added collapsed-state header rendering behavior:
  - Loading state text while markdown files are being fetched.
  - Empty state text when no root `.md` files exist.
  - Chip-style filename list when markdown files are present.
- Added supporting styles in `src/pages/ProjectActivityPage.css` for collapsed preview text and filename chips.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Markdown Filenames Moved Into Toggle Button)
- Updated `src/pages/ProjectActivityPage.tsx` so collapsed markdown filenames render inside the `Markdown Display` toggle button itself (same click target).
- Replaced invalid list-in-button structure with inline span chips so the full title/chip area toggles open/close.
- Updated `src/pages/ProjectActivityPage.css`:
  - Added `.project-activity-markdown-toggle` for full-width, wrapping button layout.
  - Kept collapsed filename chips wrapping cleanly in the button.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Remove Activity Summary Repo Line)
- Removed the `Repo: ...` row from the activity summary block in `src/pages/ProjectActivityPage.tsx`.
- Remaining summary rows still show:
  - Last pull timestamp
  - Remaining API budget

## Additional Work (Recent Commits Collapsed Timestamp Preview)
- Updated `src/pages/ProjectActivityPage.tsx` so collapsed `Recent Commits` shows the most recent commit timestamp inline in the toggle row.
- Added derived latest timestamp computation from fetched commits:
  - uses newest valid `committedAt`
  - falls back to `Unknown time` when timestamps are unavailable
- Added collapsed-state inline text:
  - `Latest: <timestamp>`
  - `No commits yet` when no commits are present
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Activity Header Title Uses Project Name)
- Removed generic activity header labels and set the main activity page title to the current project name.
- Updated `src/pages/ProjectActivityPage.tsx`:
  - header title now renders only `project.name` when present (no fallback label), preserving user-entered case.
- Removed activity subtitle text:
  - `GitHub activity, session controls, and next-step workflow.`
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Project Thumbnail Repo Link to GitHub)
- Updated project thumbnails in `src/pages/ProjectPage.tsx` so repo text is now a real GitHub link (`https://github.com/<owner>/<repo>`).
- Preserved thumbnail click-to-activity behavior by switching the card wrapper from a nested `Link` pattern to a keyboard-accessible clickable card (`role="link"`, `tabIndex`, Enter/Space handling) with router `navigate(...)`.
- Stopped click propagation on the repo anchor so clicking the repo opens GitHub instead of navigating to activity.
- Updated `src/pages/ProjectPage.css`:
  - added focus-visible outline and pointer cursor for the clickable card
  - kept repo link color consistent with existing text (`color: inherit; text-decoration: none`)
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Move Repo Link To Activity Page)
- Reverted prior `/project` thumbnail repo-link behavior and restored original thumbnail link/card structure in:
  - `src/pages/ProjectPage.tsx`
  - `src/pages/ProjectPage.css`
- Added GitHub link on `/project/$projectId/activity` current project repo line:
  - `src/pages/ProjectActivityPage.tsx` now renders `github.com/<owner>/<repo>` as an anchor to `https://github.com/<owner>/<repo>`.
- Kept the activity repo link visually consistent with existing text via:
  - `src/pages/ProjectActivityPage.css` (`color: inherit; text-decoration: none`)
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx src/pages/ProjectActivityPage.tsx` passed.

## Additional Work (Framer Motion Floating Project Thumbnails)
- Implemented zero-gravity thumbnail motion on `/project` in the `Your Projects` grid using `framer-motion`:
  - updated `src/pages/ProjectPage.tsx` to wrap each thumbnail list item with `motion.li`
  - added desynchronized floating paths (`x`, `y`, `rotate`) with infinite mirrored easing loops
  - preserved existing card click behavior to open project activity
- Added reduced-motion handling:
  - uses `useReducedMotion()` to disable float animation when motion should be minimized
- Added performance hint style:
  - `src/pages/ProjectPage.css` sets `will-change: transform` on `.project-thumb-item`
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.
  - `bun run build` fails due pre-existing `src/__lint_probe__.ts` (`unused` variable), unrelated to this feature.

## Additional Work (Floating Motion Expanded Across Page)
- Increased project thumbnail drift distance to page-scale movement:
  - updated `getProjectFloatMotion(...)` in `src/pages/ProjectPage.tsx` to use viewport-based keyframes (`vw`/`vh`) for `x` and `y`.
- Increased motion intensity:
  - larger translation ranges
  - stronger rotation
  - slightly longer loop timing for broad roaming motion
  - retained subtle scale pulsing
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Run-Around Motion Instead Of Swinging)
- Changed thumbnail motion behavior from mirrored oscillation to continuous travel loops in `src/pages/ProjectPage.tsx`.
- Updated transition settings:
  - `ease: "linear"`
  - `repeatType: "loop"` (replaces mirrored back-and-forth behavior)
- Reworked path keyframes to closed route-style paths around the viewport, so thumbnails move directionally around the page rather than swinging in place.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Push Motion Farther Right)
- Increased rightward travel distance for thumbnail loop paths in `src/pages/ProjectPage.tsx` by raising positive `x` keyframes across all float patterns.
- Result: cards now travel farther to the right side of the page during each run-around loop.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Reduce Leftward Travel)
- Tuned thumbnail run-around paths in `src/pages/ProjectPage.tsx` to move less far to the left by reducing negative `x` extremes across all motion patterns.
- Kept rightward reach intact so overall movement remains right-biased.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Edge-Constrained Run-Around Motion)
- Reworked `/project` thumbnail motion in `src/pages/ProjectPage.tsx` so cards can travel very close to viewport edges while staying on-screen.
- Implemented per-card viewport bounds measurement using `getBoundingClientRect()`:
  - stores motion constraints per project thumbnail (`minX/maxX/minY/maxY`)
  - updates on resize, scroll, and element resize (`ResizeObserver`)
- Updated motion path generation:
  - keyframes are now derived from measured bounds and scaled near limits (`edgeFactor`)
  - closed loop travel remains directional (`repeatType: "loop"`, linear timing)
  - cards get near the edge without leaving the viewport
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Less Down, More Up Motion)
- Rebalanced thumbnail run-around vertical motion in `src/pages/ProjectPage.tsx`:
  - increased upward reach toward top edge (`top` factor raised)
  - reduced downward reach (`bottom` factor lowered)
  - adjusted inner Y waypoints and route ordering to keep loops top-biased
- Result: cards travel less far down the screen and farther up, while remaining within edge constraints.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Even Higher, Even Less Low)
- Further tuned vertical run-around range in `src/pages/ProjectPage.tsx`:
  - top motion now uses full measured upward bound (`top = normalizedBounds.minY`)
  - downward bound reduced again (`bottom = normalizedBounds.maxY * 0.3`)
  - inner Y waypoints moved upward to bias loops near the top of the viewport
- Result: cards can travel much higher and stay noticeably less low on the page.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Idle Trigger For Floating Effect)
- Updated `/project` thumbnail animation behavior in `src/pages/ProjectPage.tsx` to activate only after 30 seconds of no user activity.
- Added idle detection state and listeners:
  - starts a 30s timer on page load
  - resets on page activity (`pointerdown`, `keydown`, `wheel`, `scroll`, `touchstart`, `input`)
  - when timer expires, floating animation turns on
- Added “stay in place until idle” behavior:
  - before idle threshold: cards animate to `{ x: 0, y: 0, rotate: 0, scale: 1 }`
  - after idle threshold: cards follow run-around loop paths
- Kept reduced-motion behavior safe by gating animation through `shouldAnimateThumbnails`.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Code Block Idle Animation At 31s)
- Added a second idle animation trigger for the intro code block in `src/pages/ProjectPage.tsx`:
  - project thumbnails still start at 30s idle
  - code block starts at 31s idle
- Updated shared idle handler to reset both timers/states on any page activity.
- Converted intro code block from `<pre>` to `<motion.pre>` with its own path profile.
- Added dedicated code-block motion path (`getCodeBlockFloatMotion`) designed for the upper page area with slight off-screen travel.
- Before 31s idle, code block stays in its original position (`x/y/rotate/scale` reset to neutral).
- Added render-performance hint in `src/pages/ProjectPage.css`:
  - `.project-page-intro-code { will-change: transform; }`
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Dancing Sloppy Projects Heading)
- Updated `Sloppy Projects` heading in `src/pages/ProjectPage.tsx` from static `<h1>` to `<motion.h1>` with looping dance motion:
  - position drift (`x/y`)
  - scale growth/shrink pulses
  - rotational wobble
- Added dancing border/shape treatment in `src/pages/ProjectPage.css`:
  - animated border style switching between `solid` and `dotted`
  - animated border-radius morphing through multiple shapes
  - animated border color changes to emphasize movement
- Result: heading now appears to “dance” with both transform motion and changing border/shape style.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (29s Input Collision Rhythm)
- Added a new idle-phase animation for the two form inputs on `/project`:
  - starts at 29 seconds of inactivity
  - resets immediately on any activity
- Updated idle timer logic in `src/pages/ProjectPage.tsx` to include three tiers:
  - 29s: input collision rhythm
  - 30s: project thumbnail floating
  - 31s: code-block upper-route floating
- Converted both form inputs to `motion.input` and added a steady, deterministic beat loop:
  - top input moves down
  - bottom input moves up
  - both pulse border/glow at collision beats
  - fixed cadence (`duration: 1.08`, linear loop) to keep rhythm steady (not random)
- Inputs remain in normal position before idle threshold (`x/y/scale` reset state).
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Delay Heading Dance To 31s Idle)
- Updated `Sloppy Projects` heading animation gating in `src/pages/ProjectPage.tsx`:
  - heading now animates only when the 31-second idle phase is active
  - before 31s idle, heading remains static at its base transform
- Implemented via `shouldAnimateHeading = !shouldReduceMotion && isCodeBlockFloatingEnabled`.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (33s Text Font-Cycle Phase)
- Added a fourth idle-phase trigger in `src/pages/ProjectPage.tsx`:
  - 33s idle enables text font-family cycling
  - resets to normal font immediately on any page activity
- Applied conditional class to the page container:
  - `.project-page-font-cycle`
- Added font-cycle animation in `src/pages/ProjectPage.css`:
  - cycles through requested families in order:
    - Papyrus
    - Courier
    - Serif
    - Comic Sans
  - uses the same steady beat timing as input collision:
    - `duration: 1.08s`
    - `linear`
    - `infinite`
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Fix Heading Dancing Too Early)
- Fixed `Sloppy Projects` dancing border animation running continuously from page load.
- Root cause: CSS keyframe animation was always active via `.project-page-title-dance`.
- Updated gating so heading border animation only runs when 31s idle state is active:
  - `src/pages/ProjectPage.tsx` now adds `.is-active` class only when `shouldAnimateHeading` is true.
  - `src/pages/ProjectPage.css` now applies `animation: sloppy-title-border-dance...` only under `.project-page-title-dance.is-active`.
- Result: after refresh/page load, heading stays static until the intended idle trigger.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (No Visible Heading Border Before Trigger)
- Updated `src/pages/ProjectPage.css` so `Sloppy Projects` has no visible border before idle trigger:
  - `.project-page-title-dance` now uses `border: 2px solid transparent`.
- The animated border still appears once `.is-active` is applied at the 31s idle phase.

## Additional Work (40s Panic Start-Project Button)
- Added a 40-second idle trigger in `src/pages/ProjectPage.tsx` for the Start Project button panic behavior.
- Updated idle-phase stack:
  - 29s: input collision rhythm
  - 30s: project thumbnails run-around
  - 31s: code block + heading dance trigger
  - 33s: text font-cycle
  - 40s: Start Project panic motion/text
- Converted submit button to `motion.button` and added lower-right movement:
  - computes dynamic target offset toward viewport lower-right from current button position
  - animates on a steady beat loop (`duration: 1.08`, linear, infinite)
  - includes panic pulse/jitter and glow
- Added text alternation during panic phase:
  - `WHAT ARE YOU DOING!!`
  - `HELP ME!!`
- Panic phase resets on any user activity.
- Added supporting style hook in `src/pages/ProjectPage.css`:
  - `.project-primary-button.is-panic` (`z-index`, `white-space`)
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Push All Idle Triggers Back By 20s)
- Updated idle-delay constants in `src/pages/ProjectPage.tsx` by +20 seconds across all phases.
- New trigger schedule:
  - 49s: input collision rhythm
  - 50s: project thumbnails run-around
  - 51s: code block + heading dance trigger
  - 53s: text font-cycle
  - 60s: Start Project panic motion/text
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Keep Active Thumbnail On-Screen)
- Updated project thumbnail motion in `src/pages/ProjectPage.tsx` to treat active projects with stricter bounds.
- Changes:
  - pass `project.status === "active"` into `getProjectFloatMotion(...)`
  - apply larger inset constraints for active cards via new `insetMotionBounds(...)`
  - reduce active-card rotation and scale amplitude to avoid edge overshoot
  - slightly slow active-card loop for steadier on-screen movement
- Result: active project thumbnail remains on the page while still moving.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Expanded 33s Font Cycle Families)
- Updated `@keyframes sloppy-text-font-cycle` in `src/pages/ProjectPage.css` to include additional requested fonts in the cycle.
- Current cycle now includes:
  - Papyrus
  - Courier
  - Serif
  - Comic Sans
  - Monospace
  - Roboto
  - Times New Roman
- Kept the same rhythm timing (`1.08s`, linear, infinite) to stay aligned with the input collision beat.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Remove Dash Prefixes In Anticipation Section)
- Updated the `/project` intro copy in `src/pages/ProjectPage.tsx` under `Here's what to anticipate:`
- Removed leading `-` markers from the three lines while keeping line breaks and wording.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Top Intro T-Minus Countdown)
- Updated the first intro line on `/project` in `src/pages/ProjectPage.tsx` to:
  - `This page is going to freak out in t-minus {countdown}.`
- Added a subtle inline countdown display tied to the first idle trigger timestamp:
  - introduced `idleCountdownNowMs` and `nextFreakoutAtMs` state
  - resets countdown target on any user activity in `markActivity`
  - updates countdown every `250ms` while motion is enabled
  - renders formatted `m:ss` output via `formatCountdownLabel(...)`
- Added subtle text styling in `src/pages/ProjectPage.css`:
  - `.project-page-countdown` uses soft color and tabular numerals so it blends with surrounding text.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Move Countdown Under Anticipate Section)
- Moved the `t-minus` countdown line in `src/pages/ProjectPage.tsx` from the top intro line into the `Here's what to anticipate:` paragraph.
- Countdown now renders directly under that heading and remains inline with subtle styling.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Push Idle Triggers Back Another 20s)
- Updated idle-delay constants in `src/pages/ProjectPage.tsx` by an additional +20 seconds.
- New trigger schedule:
  - 69s: input collision rhythm + countdown target
  - 70s: project thumbnails run-around
  - 71s: code block + heading dance
  - 73s: text font-cycle
  - 80s: Start Project panic motion/text
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Restore Dash Notes + Remove Bottom Two)
- Updated `Here's what to anticipate:` in `src/pages/ProjectPage.tsx` to use dash markers again.
- Kept only:
  - `- this page is going to freak out in t-minus {countdown}.`
  - `- commit messages will display in real-time`
- Removed the two bottom notes that previously mentioned the timeclock and site visibility lines.
- Validation:
  - `./node_modules/.bin/eslint src/pages/ProjectPage.tsx` passed.

## Additional Work (Fix Build Failure From Lint Probe)
- Removed temporary probe file `src/__lint_probe__.ts` that intentionally contained TypeScript/lint violations.
- Confirmed no runtime references to the probe file in the codebase.
- Validation:
  - `bun run build` passed (`tsc -b && vite build`).
